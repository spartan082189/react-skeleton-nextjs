import PropTypes from 'prop-types';
import withLayout from 'hoc/withLayout';

const Home = () => {
  return (
    <div>
     hero stoof
    </div>
  );
};

Home.propTypes = {
};

export default withLayout(Home);